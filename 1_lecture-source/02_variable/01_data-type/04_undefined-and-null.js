/* undefined 타입
   var 키워드로 선언한 변수는 암묵적으로 undefined로 초기화 된다.
*/
var undef;
console.log(undef);

/* null 타입
   변수에 값이 없다는 것을 의도적으로 명시할 때 사용한다.
*/
var nullVal = 'something';
nullVal = null;
console.log(nullVal);